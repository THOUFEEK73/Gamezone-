<div>
    <label class="block text-sm font-medium mb-2">Production Company:</label>
    <div class="flex flex-wrap gap-2">
        <% if (game.company && Array.isArray(game.company)) { %>
            <% game.company.forEach(com => { %>
                <span class="platform-badge"><%= com %></span>
            <% }) %>
        <% } else { %>
            <span class="text-gray-500">No company data available</span>
        <% } %>
    </div>
</div>
