<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Error - Oops!</title>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@500;700&display=swap');

    body {
        margin: 0;
        height: 100vh;
        background: #121212;
        display: flex;
        justify-content: center;
        align-items: center;
        font-family: 'Montserrat', sans-serif;
        color: #eee;
        overflow: hidden;
        padding: 20px;
    }

    .container {
        background: #1c1c1c;
        padding: 40px 50px;
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(255, 255, 255, 0.1);
        max-width: 480px;
        width: 100%;
        text-align: center;
        position: relative;
        border: 1px solid #333;
    }

    h1 {
        font-size: 4rem;
        margin: 0 0 20px 0;
        color: #ff3b3f; /* softer red */
        text-shadow: 0 0 12px #ff3b3f;
        animation: pulseGlow 2.5s ease-in-out infinite;
        user-select: none;
    }

    /* Pulsing glow animation for heading */
    @keyframes pulseGlow {
        0%, 100% {
            text-shadow: 0 0 12px #ff3b3f, 0 0 28px #ff3b3f;
        }
        50% {
            text-shadow: 0 0 28px #ff3b3f, 0 0 40px #ff3b3f;
        }
    }

    /* Typing effect for message */
    .message {
        font-size: 1.3rem;
        line-height: 1.5;
        color: #bbb;
        white-space: nowrap;
        overflow: hidden;
        border-right: 3px solid #ff3b3f;
        width: 0;
        margin: 0 auto 35px auto;
        animation: typing 3s steps(40, end) forwards, blink 0.75s step-end infinite;
    }

    @keyframes typing {
        from { width: 0 }
        to { width: 100% }
    }

    @keyframes blink {
        50% { border-color: transparent }
    }

    a.button {
        display: inline-block;
        padding: 14px 45px;
        font-size: 1.1rem;
        color: #121212;
        background-color: #ff3b3f;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 700;
        box-shadow: 0 6px 15px rgba(255, 59, 63, 0.7);
        transition: transform 0.25s ease, box-shadow 0.25s ease;
        user-select: none;
    }

    a.button:hover {
        transform: scale(1.05);
        box-shadow: 0 8px 25px rgba(255, 59, 63, 0.9);
    }

    a.button:active {
        transform: scale(0.95);
    }

    /* Responsive */
    @media (max-width: 480px) {
        .container {
            padding: 30px 25px;
        }
        h1 {
            font-size: 3rem;
        }
        .message {
            font-size: 1.1rem;
            margin-bottom: 30px;
        }
        a.button {
            padding: 12px 35px;
            font-size: 1rem;
        }
    }
</style>
</head>
<body>
    <div class="container">
        <h1>Oops!</h1>
        <p class="message"><%= message %></p>
        <a href="/admin/login" class="button" role="button">Go Back Home</a>
    </div>
</body>
</html>
